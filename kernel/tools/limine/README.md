Place the Limine prebuilt files here (download from https://github.com/limine-bootloader/limine/releases).

Required files:
- limine.sys
- limine-install (executable)
- limine.c32 (optional)

On Windows, download the archive for your platform and extract `limine-install.exe` and `limine.sys` into this folder.