# Инструкция по тестированию DOOM

## Запуск в QEMU (Windows)

### 1. Установка QEMU

**Способ 1: Через Chocolatey**
```powershell
# Запустите PowerShell от администратора
choco install qemu
```

**Способ 2: Вручную**
1. Скачайте QEMU для Windows: https://qemu.weilnetz.de/w64/
2. Установите в `C:\Program Files\qemu\`
3. Добавьте в PATH: `C:\Program Files\qemu\`

### 2. Запуск ospabOS с DOOM

```powershell
cd D:\ospab-projects\ospab.os\kernel\isos
qemu-system-x86_64.exe -cdrom ospab-os-44.iso -m 256M -serial stdio
```

### 3. Тестирование DOOM

1. Дождитесь загрузки (2-3 секунды)
2. Вы увидите приглашение: `[ospab]~>`
3. Введите команду: `doom`
4. Нажмите Enter
5. Вы увидите огненные эффекты и надпись "DOOM"
6. Управление:
   - W - вверх
   - S - вниз
   - A - влево
   - D - вправо
   - Space - действие
   - Ctrl+C - выход

## Запуск в QEMU (Linux/WSL)

```bash
cd /mnt/d/ospab-projects/ospab.os/kernel/isos
qemu-system-x86_64 -cdrom ospab-os-44.iso -m 256M -serial stdio
```

## Запуск на реальном железе

### USB-флешка (Windows)

```powershell
# Запустите PowerShell от администратора
cd D:\ospab-projects\ospab.os
.\install_usb.ps1
# Выберите USB-диск из списка
# Дождитесь завершения записи
```

### USB-флешка (Linux)

```bash
cd /mnt/d/ospab-projects/ospab.os
sudo bash install_usb.sh
# Укажите USB-устройство (например, /dev/sdb)
```

### Загрузка

1. Вставьте USB-флешку в компьютер
2. Перезагрузите
3. Войдите в Boot Menu (F12/F11/F8/ESC)
4. Выберите USB-устройство
5. Дождитесь загрузки ospabOS
6. Введите `doom` для запуска

## Проверка функциональности

### Тест 1: Базовые команды
```bash
[ospab]~> help      # Должен показать список команд
[ospab]~> version   # Должен показать v0.44
[ospab]~> ls /bin   # Должен показать ls, cat, grape
[ospab]~> uptime    # Должен показать время работы
```

### Тест 2: Grape редактор
```bash
[ospab]~> grape test.txt
# Введите текст
# Ctrl+X - сохранить
# Ctrl+C - выйти
[ospab]~> cat test.txt  # Должен показать сохранённый текст
```

### Тест 3: DOOM
```bash
[ospab]~> doom
# Должен появиться экран с огненными эффектами
# Надпись "DOOM" в центре
# Нажмите W/A/S/D для проверки ввода
# Ctrl+C для выхода
```

## Возможные проблемы

### QEMU не запускается

**Проблема**: `qemu-system-x86_64 не распознано`

**Решение**:
```powershell
# Проверьте установку
where.exe qemu-system-x86_64

# Если не найдено, установите:
choco install qemu
# или добавьте в PATH вручную
```

### Чёрный экран в DOOM

**Проблема**: DOOM запускается, но экран чёрный

**Причины**:
1. Framebuffer не инициализирован
2. Неправильное масштабирование
3. RGB/BGR несоответствие

**Проверка**:
- Добавьте `-serial stdio` для отладки
- Проверьте серийный вывод на ошибки

### Клавиатура не работает

**Проблема**: Клавиши не реагируют в DOOM

**Решение**:
1. Проверьте, что окно QEMU в фокусе
2. Попробуйте английскую раскладку
3. В реальном железе проверьте PS/2 клавиатуру

### DOOM сразу выходит

**Проблема**: DOOM запускается и сразу закрывается

**Причины**:
1. Случайный Ctrl+C в буфере
2. Ошибка инициализации

**Решение**:
- Проверьте серийный лог
- Перезапустите QEMU
- Попробуйте другую версию ISO

## Логи и отладка

### Серийный вывод

```powershell
# Windows (QEMU с -serial stdio)
qemu-system-x86_64 -cdrom ospab-os-44.iso -m 256M -serial stdio
# Вывод появится в консоли
```

```bash
# Linux
qemu-system-x86_64 -cdrom ospab-os-44.iso -m 256M -serial file:serial.log
# Логи в файле serial.log
```

### Проверка сборки

```bash
cd D:\ospab-projects\ospab.os\kernel
wsl bash build_with_alloc.sh

# Должно быть:
# - Build successful!
# - ISO created: ospab-os-44.iso (1.4M)
# - No errors
```

### Информация о системе

Внутри ospabOS:
```bash
[ospab]~> version   # Должно показать v0.44
[ospab]~> uptime    # Время работы в секундах
[ospab]~> ls /dev   # Должны быть: keyboard, framebuffer
```

## Бенчмарки

### Время загрузки
- QEMU: ~1-2 секунды
- VirtualBox: ~2-3 секунды
- Реальное железо (UEFI): ~3-5 секунд
- Реальное железо (BIOS): ~4-6 секунд

### Производительность DOOM
- FPS: ~30-60 (зависит от CPU)
- Разрешение: 320x200 → масштабирование на экран
- Задержка ввода: <10ms

### Использование памяти
- Kernel: ~2-3 MB
- DOOM буфер: 256 KB (320*200*4)
- Heap: ~5-10 MB при работе

## Следующие шаги

После успешного тестирования:

1. Документируйте любые баги в GitHub Issues
2. Попробуйте на разном железе
3. Сделайте скриншоты/видео
4. Поделитесь результатами!

## Контакты

Если нашли баги или есть вопросы:
- GitHub: ospab-projects/ospab.os
- Создайте Issue с описанием проблемы
- Приложите логи из серийного порта

---

**Удачного тестирования! "If it can run code, it can run DOOM"** 🎮🔥
